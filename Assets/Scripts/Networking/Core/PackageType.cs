namespace Networking
{
    public enum PackageType
    {
        Unknown = 0,
        Ack,
        Combined,
        ConnectionRequest,
        ConnectionResponse,
        Test,
        Invalid
    }
}